# 📚 Library API

Этот репозиторий содержит реализацию тестового задания: RESTful API для базового управления библиотечным каталогом.

## 📋 Краткое описание задания

Необходимо разработать API, который позволяет:

- Управлять книгами и читателями (CRUD-операции);
- Реализовать выдачу и возврат книг с бизнес-ограничениями (максимум 3 книги на читателя, учёт экземпляров и возвратов);
- Добавить регистрацию и аутентификацию библиотекарей с использованием JWT токенов;
- Обеспечить защиту всех ключевых операций через токен.

## 🛠️ Технологии

- Python 3.12+
- FastAPI
- PostgreSQL
- SQLAlchemy ORM
- Alembic
- Pydantic
- Pytest
- Docker

## 📂 Структура проекта (Краткое описание)

- `.env`, `.env-docker` — файлы с переменными окружения для локальной разработки и Docker.
- `.gitignore` — список исключений для Git.
- `alembic/` — миграции базы данных
- `docker/` — файлы для запуска проекта в Docker (скрипты, volumes для БД).
- `src/` — основной исходный код приложения:
  - `main.py` — точка входа в приложение.
  - `database.py` — настройка подключения к базе данных.
  - `exceptions.py` — кастомные исключения.
  - `config/` — конфигурационные файлы приложения и БД.
  - `dao/` — базовый класс для работы с БД (Data Access Object).
  - `books/`, `borrows/`, `readers/`, `users/` — отдельные модули с моделью, DAO, роутерами и схемами.
- `tests/` — юнит-тесты (интеграционные тесты).

- `alembic.ini` — конфигурация для Alembic.
- `docker-compose.yml` — описание Docker-сервисов для запуска проекта.
- `Dockerfile` — инструкция для сборки Docker-образа.
- `README.md` — документация проекта.
- `requirements.txt` — зависимости Python.


## 🚧 Статус

- Добавлены базовые модели (Book, Reader, Borrow, User)
- Реализованы эндпоинты для JWT авторизации
- Реализовано управление книгами(CRUD, защищено JWT)
- Реализовано управлние читателями (CRUD, защищено JWT)
- Реализована выдача книг
- Реализовано получение информации: список всех книг, список всех книг взятых читателями
- Реализовано тестирование

Проект завершен

## 🚀 Инструкция по запуску

```
# 1. Клонировать репозиторий
git clone https://github.com/w0rn3zz/library-api.git
cd library-api

# 2. Из корневой директории проекта выполните:
rm -f docker/volumes/db-data/.gitkeep

# 3. Настроить окружение
cp .env.example .env-docker

# 4. Запустить через Docker
docker-compose up --build

```
Примечаниея:
- Предполагается, что у пользователся уже установлен **`Docker`** и **`Docker-Compose`**
- Файл с переменными окружения должен назвыться **`.env-docker`**, т.к. файл с названием **`.env`** используетсяд для локальной отдладки проекта

После успешного запуска по адресу **`http://127.0.0.1:8000/docs`** будет доступная документация (Swagger)

Чтобы зарегестировать первого пользователя необходимо зайти в ручку **`/auth/register`** и выполнить запрос, подстваив свои данные

Либо прописать в консоли команду
```
curl -X POST "http://localhost:8000/auth/register" \
-H "Content-Type: application/json" \
-d '{"email": "admin@library.com", "password": "securepassword", "username": "admin"}'
```

## 🗄️ Структура БД

**Ключевые таблицы**:

**books** - информация о книгах

**readers** - данные читателей

**borrows** - история выдачи (связь books-readers)

**users** - учетные записи библиотекарей

**Такая схема БД отлично подходит для дальнейшей реализации бизнес логики API**

## 👨‍💼 Реализация бизнес логики и сложности

### Выдача и возврат книг

- **Выдача книги (`borrow_book`):**  
  Проверяется наличие книги и доступных экземпляров. Проверяется, что читатель существует и не превысил лимит в 3 активных книги. Если все условия соблюдены, создаётся запись в таблице `Borrow`, а количество экземпляров книги уменьшается на 1.

- **Возврат книги (`return_book`):**  
  Находит активную выдачу книги для данного читателя (с `return_date = NULL`). При успешной проверке проставляется дата возврата, количество экземпляров увеличивается на 1. Возврат невыданных или уже возвращённых книг запрещён.

- **Получение активных выдач (`get_reader_active_borrows`):**  
  Возвращает список всех активных (не возвращённых) книг конкретного читателя для контроля лимита и отображения текущих займов.

---

### 💡 Особенности реализации и решённые сложности

- Все операции выполняются в рамках атомарных транзакций с использованием асинхронных сессий SQLAlchemy.  
  **Сложность:** необходимо было гарантировать, что все связанные объекты (книга, читатель, запись о выдаче) загружаются и изменяются в одной сессии, чтобы избежать расхождения данных.  
  **Решение:** вся бизнес-логика реализована внутри одной сессии `async_session()`, что позволяет работать с объектами в рамках одной транзакции и праивльно фиксировать изменения.

- Это гарантирует целостность данных и предотвращает расхождения между количеством экземпляров книг и записями о выдаче.

- Исключения помогают своевременно выявлять нарушения бизнес-правил (например, превышение лимита выдачи), обеспечивая надёжную обработку ошибок и информирование вызывающего кода.


---
## 🔐 Реализация аутентификации

Для аутентификации пользователей используется JWT-токены и безопасное хеширование паролей.

- **Хэширование паролей:**  
  Пароли хранятся в базе данных в виде хэшей, созданных с помощью библиотеки `bcrypt`. Это надёжный и широко используемый способ защиты паролей.

- **Проверка пароля:**  
  При входе пароль пользователя проверяется функцией `verify_password`, которая сравнивает введённый пароль с сохранённым хэшем.

- **Аутентификация пользователя:**  
  Функция `authenticate_user` загружает пользователя по email и проверяет пароль. Если данные корректны, возвращает объект пользователя, иначе — `False`.

- **Генерация JWT-токена:**  
  Функция `create_access_token` создаёт JWT с полезной нагрузкой (`data`) и временем жизни (`expires_delta`). Токен подписывается секретным ключом с использованием алгоритма, заданного в настройках. Время действия по умолчанию — 360 дней (для удобства при разработке, в целях безопасности можно уменьшить).

- **Защищённые эндпоинты:**  
Защинены эндпоинты для управления книгами, читателями, а также эндпоинты для выдачи и возврата книг. Защищен эндпоинт для получения списка всех книг, т.к. эндпоинт для получения одной книги должен быть защищен. Все эти эндпоинты защищены, т.к. этот API предназначен для библиотекарей, а не для общего пользования.

- **Используемые библиотеки:**  
  - `bcrypt` — для безопасного хеширования и проверки паролей.  
  - `jwt` (PyJWT) — для генерации и проверки JWT-токенов. 

Эти библиотеки широко используются Python-разработчиками и имеют хорошую документацию

---

## 📋 Дополнительная фича, которую я бы добавил в API — Резервирование книг

Я считаю, что очень полезной функциональностью станет возможность резервировать книги читателями.

### Зачем это нужно?

Если книга пользуется высоким спросом, читатель сможет заранее её зарезервировать, чтобы гарантировать доступ к ней, когда придёт время взять.

### Как я бы это реализовал?

- **База данных:**  
  Создать отдельную таблицу `reserved_books`, где хранится связь между читателем (`reader_id`) и книгой (`book_id`). В таблице также предусмотреть поля:  
  - `reserv_expire_date` — дата, до которой действует резервирование  
  - `is_reserv_active` — флаг активности резервации

- **DAO слой:**  
  Для работы с этой таблицей создать отдельный DAO-класс `ReservDAO`, отвечающий за добавление, обновление и удаление резервирований.

- **Эндпоинт:**  
  Добавить защищённый JWT-эндпоинт, позволяющий авторизованному читателю создавать резервирование книги.

- **Бизнес-логика:**  
  Когда читатель приходит взять ранее зарезервированную книгу, резерв автоматически снимается:  
  - `is_reserv_active` устанавливается в `False`  
  - `reserv_expire_date` обновляется на текущую дату  

  Это гарантирует, что резерв не будет висеть после выдачи книги.

---

## 👤 Автор

w0rn3zz

📅 Начало разработки: 19.06.2025

